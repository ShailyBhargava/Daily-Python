# takeout length of last word of the string

def last_word_len(A):
    a = A.split()
    #print(a)
    last_word_len = a[-1]
    print( last_word_len)

A ='handsome is called jin'
last_word_len(A)